# Example environment variables for deployment
# Set these in Render dashboard or your hosting provider

# Port the server will listen on (Render injects this automatically)
PORT=3001

# Secret used to sign JWT tokens - MUST be set in production
# NOTE: Do NOT store a real secret in this file. Set the secret in Render's
# Environment settings. Replace the value below with a placeholder.
SECRET_KEY=REPLACE_WITH_STRONG_SECRET

# Optional: path to the SQLite database file (useful if you want custom path)
DATABASE_PATH=./server/database.sqlite

# Node environment
NODE_ENV=production
